server:
  port: 9092

spring:
  application:
    name: EMPLOYEE-SERVICE
eureka:
  client:
     register-with-eureka: true
     fetch-registry: true
     serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost